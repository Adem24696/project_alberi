# Monumental Trees of Sicily — How to Run

This project is a Python data-analysis program that reads the dataset of Sicilian monumental trees, produces charts and statistics, runs KMeans clustering, and analyzes bounding boxes.

## 1) Requirements

- Python 3.10+ (recommended)
- Installed libraries:
  - scikit-learn
  - matplotlib

Install dependencies:

```bash
pip install scikit-learn matplotlib
